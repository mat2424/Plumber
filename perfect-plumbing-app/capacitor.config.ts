import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.perfectplumbing.app',
  appName: 'Perfect Plumbing App',
  webDir: 'dist'
};

export default config;
